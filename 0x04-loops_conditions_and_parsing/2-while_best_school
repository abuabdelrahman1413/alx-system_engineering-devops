#!/usr/bin/env bash
# Using a While loop to display "Best School" 10 times

# Using a While loop to display "Best School" 10 times
i=0
while [ $i -lt 10 ]
do
    echo "Best School"
    # Increment the counter
    i=$((i + 1))
done